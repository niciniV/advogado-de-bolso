# base_frontend - React Frontend

## Purpose

React + Vite + TypeScript frontend for "Advogado de Bolso". Single-page application that talks to the FastAPI backend (see `../src/advogado_de_bolso/api.py`) over HTTP. The backend serves the production build from `../base_frontend/dist/`; the Vite dev server proxies `/api/*` to the FastAPI server on port 8000 (per `.opencode/plans/10-frontend-build-and-config.md`, ISSUE-DS-003).

## Ownership

- `vite.config.ts` - Vite + Vitest config (proxy, jsdom test environment, global test setup).
- `tsconfig.json` - TypeScript compiler config (Vitest global types, jsdom matcher augmentations).
- `package.json` - Frontend deps + npm scripts (`dev`, `build`, `preview`, `lint`, `test`, `clean`).
- `package-lock.json` - Reproducible install (committed; the `Makefile` target uses `npm ci`).
- `index.html` - Vite HTML entry.
- `src/main.tsx` - React root mount (`StrictMode` + `createRoot`).
- `src/App.tsx`, `src/components/*.tsx` - Application UI (deferred to batch 7).
- `src/types.ts` - Shared TypeScript types for `Case`, `ChatMessage`, `Deadline`, etc.
- `src/defaults.ts` - Default cases and preferences (batch 7).
- `src/api.ts` - HTTP client to the FastAPI backend (batch 7).
- `src/api.test.ts`, `src/App.test.tsx` - Vitest tests (batch 7).
- `src/test/setup.ts` - Vitest setup (loads `@testing-library/jest-dom/vitest`).
- `assets/` - Static assets served by Vite.
- `.env.example` - Sample env vars (no `GEMINI_API_KEY` - the FastAPI server reads its own env).

## Local Contracts

- **Build is Vite-only** (batch 6). The previous `server.ts` (Express + `@google/genai`) was deleted in batch 7's source-code refactor; the rewritten `package.json` drops `tsx`, `esbuild`, `@types/express`, `express`, `dotenv`, `motion`, and `@google/genai`. The `start` script is removed - the FastAPI server now serves the build in production.
- **`@types/node` is RETAINED** in `devDependencies` (ISSUE-USR-014) because `vite.config.ts` still imports `path`, uses `__dirname`, and reads `process.env.DISABLE_HMR`. `npm run lint` (`tsc --noEmit`) also needs Node ambient types. Removing `@types/node` would break the lint gate.
- **Dev-server proxy**: in `npm run dev`, requests to `/api/*` are forwarded to `http://localhost:8000` (the FastAPI server). This is configured in `vite.config.ts`'s `server.proxy` block. No `VITE_API_BASE_URL` env var is needed.
- **Vitest globals**: `tsconfig.json` includes `["vitest/globals", "@testing-library/jest-dom"]` so `describe`/`it`/`expect` and jest-dom matchers are typed without imports. `vite.config.ts` sets `test.globals: true` to match. `src/test/setup.ts` loads `@testing-library/jest-dom/vitest` once per test run.
- **Test command** (`npm run test`) is wired to `vitest run`. Until batch 7 adds test files, the command exits with code 1 and prints "No test files found" - this is the expected batch 6 state, not a defect.
- **`npm run lint`** runs `tsc --noEmit` and must pass. `server.ts` is currently excluded via `tsconfig.json`'s `exclude` field because the file still exists in batch 6 but its imports (`express`, `@google/genai`, `dotenv`) were removed from `package.json`. Batch 7 will delete `server.ts` entirely; the exclude entry becomes a no-op.
- **`npm run build`** runs `vite build` and writes to `dist/`. The output is plain static assets (HTML + JS + CSS); no Node server is bundled.
- **CORS**: the FastAPI server (see `../src/advogado_de_bolso/api.py`) allows `["GET", "POST", "PATCH", "DELETE"]` from configured origins. Frontend code does not need a custom CORS client.
- **TypeScript strict mode** is NOT enabled in `tsconfig.json` (the project ships without `"strict": true`); the `tsc --noEmit` lint gate validates parse + type-resolution only. Batch 7 may add stricter checks if needed.
- **`@vitejs/plugin-react`** is in `dependencies` (not `devDependencies`) to match the pre-batch-6 layout. The duplication is harmless - npm dedupes during install. Batch 7+ may clean this up.

## Work Guidance

### File map

| File | Purpose |
|------|---------|
| `vite.config.ts` | Vite + Vitest config. `server.proxy` forwards `/api` to `http://localhost:8000`. `test.environment: "jsdom"`, `test.setupFiles: ["./src/test/setup.ts"]`, `test.globals: true`. |
| `tsconfig.json` | TypeScript compiler options. `types: ["vitest/globals", "@testing-library/jest-dom"]` enables Vitest global type checking. `exclude` includes `server.ts` (transitional, batch 7 deletes it). |
| `package.json` | npm scripts and deps. Scripts: `dev` (`vite`), `build` (`vite build`), `preview` (`vite preview`), `clean` (`rm -rf dist`), `lint` (`tsc --noEmit`), `test` (`vitest run`). |
| `package-lock.json` | Generated by `npm install` after the package.json rewrite. Committed for reproducible `npm ci` installs. |
| `src/main.tsx` | React 19 root. `createRoot(document.getElementById('root')!).render(<StrictMode><App /></StrictMode>)`. |
| `src/App.tsx` | Top-level React component. **NOT YET REFACTORED** - batch 7 rewrites this and adds the API client integration. |
| `src/components/*.tsx` | UI components (`ChatInterface`, `HomeDashboard`, `CasesList`, `ProfilePreferences`). **NOT YET REFACTORED** - batch 7 renames `isLoading` to `isSendingMessage` and wires the API client. |
| `src/types.ts` | Shared `Case`, `ChatMessage`, `Deadline`, `UserProfile`, `SystemStatus`, `AppPreferences` types. Batch 7 adds `is_demo?` to the `Case` type. |
| `src/defaults.ts` | **NEW (batch 7).** `initialPreferences` and `seedCases` (moved from inline `App.tsx` definitions). |
| `src/api.ts` | **NEW (batch 7).** Typed HTTP client for `POST /api/chat/structured`, `GET/PATCH/DELETE /api/cases/{id}`, `GET /api/cases/{id}/history`. |
| `src/api.test.ts` | **NEW (batch 7).** Vitest tests for the API client. |
| `src/App.test.tsx` | **NEW (batch 7).** Vitest integration tests for the `App` component. |
| `src/test/setup.ts` | **NEW (batch 6).** Single line: `import "@testing-library/jest-dom/vitest";` |

### Adding a new frontend test

1. Create `src/<name>.test.tsx` (or `.test.ts`).
2. Vitest picks it up via the default `include: ["**/*.{test,spec}.?(c|m)[jt]s?(x)"]` pattern.
3. Use the global `describe` / `it` / `expect` from `vitest/globals` - no imports needed.
4. For component tests, import `{render, screen} from '@testing-library/react'` and use `screen.getByRole(...)` etc. The `jest-dom` matchers (`toBeInTheDocument`, `toHaveTextContent`, etc.) are available globally.

### Adding a new API endpoint integration

1. Update `src/api.ts` (batch 7) with a typed wrapper.
2. The Vite dev server will automatically forward `/api/*` calls to `http://localhost:8000` via `server.proxy`. No rebuild needed for dev iteration.
3. In production, the FastAPI server hosts `dist/` and serves the same `/api/*` paths - no proxy needed.

## Verification

- `npm install` - install all deps (uses `package-lock.json` on `npm ci`).
- `npm run lint` - `tsc --noEmit`; must pass for the batch 6 gate.
- `npm run build` - `vite build`; must succeed (produces `dist/` for batch 7+ integration).
- `npm run test` - `vitest run`; will fail with "No test files found" until batch 7 adds tests.
- `npm run dev` - Vite dev server on port 5173 with `/api` proxy to port 8000.
- `npm run preview` - serve the production build locally (port 4173 by default).
- `npm run clean` - remove `dist/`.

## Cross-references

- Backend API endpoints consumed by the frontend: `../src/advogado_de_bolso/api.py` (the `POST /api/chat/structured`, case-management endpoints).
- Backend wire types: `../src/advogado_de_bolso/schemas.py` (the `StructuredChatRequest/Response`, `CaseSummary`, `CaseResponse`, `ChatMessage`, `UpdateCaseRequest`).
- Plan reference: `.opencode/plans/10-frontend-build-and-config.md` (the build-config spec) and `.opencode/plans/13-frontend-app.md` / `.opencode/plans/14-frontend-tests.md` (the batch 7 source-code rewrites).
